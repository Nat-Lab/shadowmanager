nguage: bash

sudo: false

addons:
  apt:
    sources:
    - debian-sid
    packages:
    - shellcheck

script:
 - bash -c 'shopt -s globstar; shellcheck $(egrep -lir '#!/bin/bash' .)'

matrix:
  fast_finish: true
